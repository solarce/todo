language: go

go:
  - 1.5.1

before_install:
  - go get github.com/axw/gocov/gocov
  - go get github.com/mattn/goveralls
  - go get golang.org/x/tools/cmd/cover

script:
  - pwd
  - goveralls
  - goveralls -package=$TRAVIS_BUILD_DIR/internal/task/ -service=travis-ci
